<!doctype html>
<html lang="id">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Generate Nabr Link - Form</title>
  <style>
    :root{
      --bg:#f4f4f6;
      --card:#fff;
      --muted:#666;
      --accent:#178f59;
      --input:#f7f8fa;
      --border:#d7d7d9;
      font-family: Inter, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
    }
    body{
      margin:0;
      background: linear-gradient(180deg, #ededf1 0%, var(--bg) 100%);
      color:#111;
      padding:18px;
    }

    /* Top "browser" bar */
    .browser {
      display:flex;
      align-items:center;
      gap:12px;
      background:#ddd;
      padding:8px 12px;
      border-radius:6px;
      margin-bottom:18px;
      box-shadow:0 1px 0 rgba(0,0,0,0.06);
    }
    .nav-icons { display:flex; gap:6px; align-items:center; }
    .dot { width:12px; height:12px; border-radius:50%; background:#fff; border:1px solid #bbb; }
    .url {
      flex:1;
      background:#fff;
      padding:8px 12px;
      border-radius:6px;
      border:1px solid var(--border);
      font-size:14px;
      color:#333;
    }

    /* Main container */
    .container {
      max-width:980px;
      margin:0 auto;
      background:transparent;
    }

    /* navbar under */
    .menu {
      display:flex;
      gap:14px;
      align-items:center;
      padding:10px 6px;
      margin-bottom:14px;
      background:transparent;
    }
    .menu a { color:var(--muted); text-decoration:none; padding:6px 10px; border-radius:4px; }
    .menu a.active { background:#fff; border:1px solid var(--border); color:#222; }

    /* form layout */
    .form-wrap {
      display:grid;
      grid-template-columns:220px 1fr;
      gap:20px;
      align-items:start;
      background:rgba(255,255,255,0.02);
    }

    .img-area {
      background:var(--card);
      border:1px dashed var(--border);
      height:260px;
      display:flex;
      flex-direction:column;
      align-items:center;
      justify-content:center;
      gap:10px;
      padding:12px;
      border-radius:8px;
    }
    .img-area .placeholder {
      width:100%;
      height:180px;
      background:linear-gradient(90deg,#fff,#f8f8f8);
      border:1px solid var(--border);
      display:flex;
      align-items:center;
      justify-content:center;
      color:var(--muted);
      font-size:14px;
    }
    .btn {
      display:inline-block;
      padding:8px 12px;
      background:#fff;
      border:1px solid var(--border);
      border-radius:6px;
      cursor:pointer;
      font-size:14px;
    }

    form { background:var(--card); padding:18px; border-radius:10px; box-shadow: 0 6px 18px rgba(13,13,13,0.04); }

    .field-row {
      display:flex;
      gap:12px;
      align-items:flex-start;
      margin-bottom:12px;
    }
    label {
      width:140px;
      font-weight:600;
      color:var(--muted);
      font-size:14px;
      padding-top:6px;
    }

    .field {
      flex:1;
    }

    input[type="text"],
    input[type="number"],
    input[type="email"],
    select,
    textarea {
      width:100%;
      box-sizing:border-box;
      padding:10px 12px;
      border-radius:6px;
      border:1px solid var(--border);
      background:var(--input);
      font-size:14px;
      color:#111;
    }
    textarea { min-height:96px; resize:vertical; }

    .inline {
      display:flex;
      gap:8px;
      align-items:center;
    }

    .small {
      width:72px;
    }

    .checkbox {
      display:flex;
      gap:10px;
      align-items:center;
      margin-top:6px;
    }

    .hint { font-size:12px; color:var(--muted); margin-top:6px; }

    .generate {
      display:flex;
      justify-content:center;
      margin-top:14px;
    }
    .generate button {
      background:var(--accent);
      color:#fff;
      padding:12px 18px;
      border:none;
      border-radius:8px;
      font-weight:700;
      cursor:pointer;
      box-shadow:0 4px 10px rgba(23,143,89,0.18);
    }

    /* responsive */
    @media (max-width:820px){
      .form-wrap { grid-template-columns:1fr; }
      label{ width:120px; }
      .img-area{ order:1; }
      form{ order:2; margin-top:12px; }
    }
  </style>
</head>
<body>

<div class="container">
  <div class="browser" role="region" aria-label="browser bar">
    <div class="nav-icons" aria-hidden="true">
      <div class="dot"></div><div class="dot"></div><div class="dot"></div>
    </div>
    <div class="url" contenteditable="true">http://</div>
  </div>

  <nav class="menu" aria-label="main menu">
    <a href="#" class="active">Home</a>
    <a href="#">Generate Link</a>
    <a href="#">Transaction History</a>
    <a href="#">Policy</a>
    <a href="#">Logout</a>
  </nav>

  <div class="form-wrap">
    <!-- left: image -->
    <div class="img-area" aria-hidden="false">
      <div class="placeholder" id="preview">Preview Image</div>
      <label class="btn" for="upload">Upload Picture
        <input id="upload" type="file" accept="image/*" style="display:none" />
      </label>
    </div>

    <!-- right: form -->
    <form id="nabrForm" onsubmit="return handleSubmit(event)" aria-label="Form generate link">
      <div class="field-row">
        <label for="title">Title Here :</label>
        <div class="field">
          <input id="title" name="title" type="text" placeholder="MacBook Air £800" value="MacBook Air £800">
        </div>
      </div>

      <div class="field-row">
        <label for="desc">Description :</label>
        <div class="field">
          <textarea id="desc" name="description" placeholder="Year : 2009\nModel : \nCondition : Mint">Year : 2009
Model : 
Condition : Mint</textarea>
        </div>
      </div>

      <div class="field-row">
        <label for="account">Account Number :</label>
        <div class="field">
          <input id="account" name="account" type="text" placeholder="123-456-789-0">
        </div>
      </div>

      <div class="field-row">
        <label for="bank">Bank :</label>
        <div class="field inline">
          <select id="bank" name="bank">
            <option value="">-- Pilih Bank --</option>
            <option>HSBC</option>
            <option>BNI</option>
            <option>BCA</option>
            <option>Mandiri</option>
            <option>BRI</option>
          </select>
        </div>
      </div>

      <div class="field-row">
        <label for="holder">Account Holder :</label>
        <div class="field">
          <input id="holder" name="holder" type="text" placeholder="Name Here">
        </div>
      </div>

      <div class="field-row">
        <label for="transaction">Transaction :</label>
        <div class="field inline">
          <input id="transaction" name="transaction" type="number" min="1" class="small" placeholder="1">
          <div class="hint">* jika transaksi untuk 3 orang pilih 3 dari daftar</div>
        </div>
      </div>

      <div class="field-row">
        <label>&nbsp;</label>
        <div class="field">
          <div class="checkbox">
            <label><input type="checkbox" id="sendPhoneToggle"> Send to Mobile Phone</label>
            <label><input type="checkbox" id="sendEmailToggle"> Send to Email</label>
          </div>
        </div>
      </div>

      <div class="field-row" id="phoneRow" style="display:none;">
        <label for="phone">Send to Mobile Phone :</label>
        <div class="field">
          <input id="phone" name="phone" type="text" placeholder="081234567890, 081234567891">
        </div>
      </div>

      <div class="field-row" id="emailRow" style="display:none;">
        <label for="email">Send to Email :</label>
        <div class="field">
          <input id="email" name="email" type="email" placeholder="mfm...@soton.ac.uk, fys...@yahoo.com">
        </div>
      </div>

      <div class="generate">
        <button type="submit" id="generateBtn">Generate Nabr Link</button>
      </div>
      <div class="form-row">
        <label for="phone">Send to Mobile Phone:</label>
        <input type="text" id="phone" name="phone" placeholder="Masukkan nomor HP"/>
      </div>
      <div class="form-row">
        <label for="email">Send to Email:</label>
        <input type="email" id="email" name="email" placeholder="Masukkan email">
      </div>
    </form>
  </div>
</div>

</body>
</html>
